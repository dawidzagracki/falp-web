node_modules
dist
dist-ssr
.vite
*.local
.DS_Store
.env
.env.local
.env.nowa
deploy/.env.nowa

# Paczki / archiwa deployu
*.tgz
*.tar.gz

# Surowe źródła realizacji (przetwarzane skryptem do public/realizacje)
_realizacje_src/

# Notatki wewnętrzne (szczegóły infrastruktury — nie publikować w publicznym repo)
HANDOFF.md

# Docker
.docker/

# lokalne zgloszenia z formularza (na serwerze trzymane w wolumenie)
www/blog/zgloszenia.json
